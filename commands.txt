On branch station-upgrade
nothing to commit, working tree clean
The log files do not appear beacuse the .gitignore file tells Git to ignore files that has names that matchs log(number).txt.
diff --git a/commands.txt b/commands.txt
index e69de29..baa0338 100644
--- a/commands.txt
+++ b/commands.txt
@@ -0,0 +1,3 @@
+On branch station-upgrade
+nothing to commit, working tree clean
+The log files do not appear beacuse the .gitignore file tells Git to ignore files that has names that matchs log(number).txt.
diff --git a/results/summary.md b/results/summary.md
index 198b22c..c653666 100644
--- a/results/summary.md
+++ b/results/summary.md
@@ -1 +1,2 @@
 Analysis of the first field station.
+      41
diff --git a/scripts/03-plot.py b/scripts/03-plot.py
index 70ea841..c5af7da 100644
--- a/scripts/03-plot.py
+++ b/scripts/03-plot.py
@@ -1,3 +1,4 @@
+# Reviewed for the field season report
 # Plot the daily temperature readings
 # Prints a simple text chart, one bar per reading.
 
Lines starting with + show content that was added in the new version of the file.
b47cfc0 Update analysis
fcac182 Add gitignore
460ca63 Update station files
d9e27ae Add results directory
a22b29e docs(readme): note committing commands.txt before task 18 and force push for bonus 2
b645f85 feat: quiz 01 starter files

#cloning the fork
pwd
git clone https://github.com/alyssa-chai/datasci350-quiz01
touch commands.txt
pwd
git status

#1
git switch -c station-upgrade
#2
mkdir results
#3
touch results/summary.md
echo "Analysis of the first field station." > results/summary.md
#4
git add results
git commit -m "Add results directory"
#5
mv data/sensor-readings.csv data/station-data.csv
#6
mkdir scripts/archive && cp scripts/*.py scripts/archive/
#7
rm docs/notes.md
#8
git add .
git commit -m "Update station files"
#9
touch .gitignore
echo "temp/" >> .gitignore
echo "log?.txt" >> .gitignore
#10
cat .gitignore
#11
git add .gitignore && git commit -m "Add gitignore"
#12
touch log{1..4}.txt
#13
git status
git status >> commands.txt
echo "The log files do not appear beacuse the .gitignore file tells Git to ignore files that has names that matchs log(number).txt." >> commands.txt
#14
wc -l < data/station-data.csv >> results/summary.md
cat results/summary.md
#15
nano scripts/03-plot.py
git diff
git diff >> commands.txt
echo "Lines starting with + show content that was added in the new version of the file." >> commands.txt
#16
git add .
git commit -m "Update analyss"
#17
git commit --amend -m "Update analysis"
git status
#18
git switch main
git merge station-upgrade
#19
git log --oneline
git log --oneline >> commands.txt
#20
cat commands.txt
nano commands.txt
